# --- API Proxy Server (server.py) ---

# Presage and Eleven Labs API keys
Presage='Your API key'
Eleven_Labs='Your Eleven Labs API key'
Gemini_Key=""

# Optional: override default host/port when running with python server.py
HOST=0.0.0.0
PORT=8000

# --- ESP32 Server (esp32_server.py) ---
# Optional: override default port
ESP32_SERVER_PORT=8001

# --- CORS (optional) ---
# Comma-separated origins to allow (defaults: localhost:3000, localhost:5173)
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173
