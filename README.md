# Piano-Tiles

# Setup

# BACKEND (NO CD)
MAC
1. python3 -m venv venv
2. source venv/bin/activate

# ALL
1. CREATE .env
2. PASTE KEYS
3. pip install -r requirements.txt
4. python3 -m uvicorn server:app --reload

# FRONTEND (cd frontend)
1. npm install
2. npm run dev

# env
Eleven_Labs=''
GEMINI_API_KEY=''
HOST=0.0.0.0
PORT=8000
ESP32_SERVER_PORT=8001

